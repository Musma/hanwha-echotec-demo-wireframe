# 프로젝트 규칙

## 테이블 정렬 (Table Alignment)
- `th` (헤더): 중앙 정렬
- `td` (일반 셀): 좌측 정렬
- 수치 데이터 (길이·폭·높이·중량·면적·금액·수량 등): **우측 정렬** — 자릿수를 맞춰 크기 비교를 쉽게
- 수치형 식별자 (ID, 코드, 지번, 전화번호, 날짜 등): 좌측 정렬
- 모든 숫자 표시 영역에 `font-variant-numeric: tabular-nums;` 적용
